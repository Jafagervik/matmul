#include "mandel.hpp"
#include <algorithm>

template <typename S>
void mandelbrot(std::array<int32_t, 5> img, uint32_t xdim, uint32_t ydim) {
    std::for_each(img.begin(), img.end(), [&]() {});
    return;
}
